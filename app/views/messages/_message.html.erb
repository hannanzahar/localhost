<li class="<%=  self_or_other(message) %>">
<!-- link the avatar of the profile pic-->
  <div class="chatavatar">
    <%= image_tag(message_interlocutor(message).avatar.url , class: "media-object pull-left", height:"70", width:"70" )  %>
  </div>
  <div class="chatboxmessagecontent">
    <p><%= message.body %></p>
    <time datetime="<%= message.created_at %>" title="<%= message.created_at.strftime("%d %b  %Y at %I:%M%p") %>">
      <%= message_interlocutor(message).first_name %> • <%= message.created_at.strftime("%H:%M %p") %>
    </time>
  </div>
</li>