<!-- NAV BAR -->
<nav class="navbar-default stuckMenu isStuck" role="navigation">
  <div class="container" style="padding-bottom: 5px;margin-left: 0px;margin-right: 0px;">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/welcome/index">Local Host</a>
    </div>

    <div class="collapse navbar-collapse navbar-right navbar-ex1-collapse">
      <ul class="nav navbar-nav">

        <li class="menuItem">
          <div class="search-nav">
            <br>
            <% unless signed_in? %>
            <% else %>
              <%= form_tag users_path, :method => :get do %>
              <%= text_field_tag :search, params[:search], class: "search-nav", placeholder:"Find New Friends Nearby" %>
              <%= submit_tag "Find Now", :name => nil, class: "search-nav-btn" %>
            <% end %>
            <% end %>
          </div>
        </li>
        <li class="menuItem"><a href="/users" id="nav-btn">Chat</a></li>

        <li class="menuItem"><a href="/users/<%= current_user.id %>" id="nav-btn"> Your Profile</a></li>

        
        <% if signed_in? %>
          <li class="menuItem" style="margin-top: 10px;" id="nav-btn"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></li>
        <% else %> 
          <li class="menuItem" style="margin-top: 10px;" id="nav-btn"><%= link_to 'Sign In', new_user_session_path %></li> 
        <% end %>
      </ul>
    </div>
  </div>
</nav> 

